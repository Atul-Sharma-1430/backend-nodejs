console.log("Starting with Express JS");

console.log("Step 1: Install Express JS in the folder using npm i express");

console.log("This will create the node_modules folder in the project, which stores Express and its dependencies");

console.log("This will also create package-lock.json, which stores information about the exact versions and dependency tree");

console.log("This will also add Express as a dependency in the package.json file");